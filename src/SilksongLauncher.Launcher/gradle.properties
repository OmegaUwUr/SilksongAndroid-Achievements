# Gradle convenience knobs. Mirrors the values Unity uses for its own
# Android builds so the AAR produced here is byte-compatible.
org.gradle.jvmargs=-Xmx4g
android.useAndroidX=true
kotlin.code.style=official

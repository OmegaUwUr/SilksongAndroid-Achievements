<?xml version="1.0" encoding="utf-8"?>
<!--
  The focus ring for controls that sit on a DARK fill: Launch, which is red,
  and View Logs, which is transparent on a near-black page.

  White here for the same reason red is used on the light buttons: it is the
  one colour that reads against what is behind it. See focus_on_light.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="4dp"
                android:color="@color/text_primary" />
            <corners android:radius="4dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</selector>
